#include <stdio.hi>
/**
*main - A programe that prints a line using a printf function
*Return: o (success)
*/

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (o);
}
